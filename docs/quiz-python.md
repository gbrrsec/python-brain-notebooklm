# Quiz Python

## Introdução

Este quiz foi desenvolvido com base nos principais conceitos da linguagem Python, abrangendo fundamentos, sintaxe, estruturas de dados, funções, programação orientada a objetos e boas práticas de desenvolvimento.

---

# Perguntas

### 1. Quem foi o criador da linguagem Python e em que ano ela foi lançada publicamente?

### 2. Qual é a origem do nome "Python"?

### 3. Python é uma linguagem compilada ou interpretada?

### 4. O que caracteriza a tipagem dinâmica no Python?

### 5. Quais são as principais regras para nomeação de variáveis?

### 6. Qual é a principal diferença entre uma lista (`list`) e uma tupla (`tuple`)?

### 7. Qual função é utilizada para capturar dados digitados pelo usuário?

### 8. Qual função permite verificar o tipo de um dado?

### 9. Como Python organiza os blocos de código?

### 10. Qual operador retorna apenas o quociente inteiro de uma divisão?

### 11. Qual é a função da instrução `elif`?

### 12. Qual método adiciona um elemento ao final de uma lista?

### 13. Qual a diferença entre os operadores `=` e `==`?

### 14. O que acontece quando a instrução `return` é executada dentro de uma função?

### 15. O que representa o parâmetro `self` em Programação Orientada a Objetos?

### 16. Quais estruturas são utilizadas para tratamento de erros?

### 17. O que é a PEP 8?

### 18. Qual estrutura de dados armazena informações em pares de chave e valor?

### 19. O que é uma função `lambda`?

### 20. Como visualizar o Zen do Python diretamente no interpretador?

---

# Gabarito

### 1.
Guido van Rossum criou a linguagem Python e lançou sua primeira versão estável em 1991.

### 2.
O nome foi inspirado no grupo humorístico britânico **Monty Python**.

### 3.
Python é uma linguagem **interpretada**.

### 4.
O tipo da variável é definido automaticamente durante a execução, sem necessidade de declaração prévia.

### 5.
- Deve começar com letra ou `_`
- Não pode conter espaços
- Não pode utilizar palavras reservadas da linguagem

### 6.
Listas são mutáveis, enquanto tuplas são imutáveis.

### 7.
A função:

```python
input()
```

### 8.
A função:

```python
type()
```

### 9.
Através da **indentação**, utilizando espaços em branco para definir blocos.

### 10.
O operador:

```python
//
```

### 11.
Permite testar uma nova condição caso a condição anterior seja falsa.

### 12.
O método:

```python
append()
```

### 13.
- `=` → atribuição de valor
- `==` → comparação de igualdade

### 14.
A função é encerrada e um valor pode ser retornado ao código que a chamou.

### 15.
Representa a instância atual do objeto.

### 16.
Os blocos:

```python
try
except
```

### 17.
É o guia oficial de estilo do Python, contendo boas práticas de desenvolvimento.

### 18.
O **dicionário (`dict`)**.

### 19.
Uma função anônima escrita em uma única linha.

Exemplo:

```python
quadrado = lambda x: x ** 2
```

### 20.
Executando o comando:

```python
import this
```

---

# Resultado

### 18 a 20 acertos
Excelente domínio dos conceitos fundamentais de Python.

### 14 a 17 acertos
Bom conhecimento da linguagem, com alguns pontos para aprofundar.

### 10 a 13 acertos
Conhecimento intermediário, recomenda-se revisar alguns tópicos.

### Menos de 10 acertos
Recomenda-se revisar os fundamentos da linguagem antes de avançar para conteúdos mais complexos.